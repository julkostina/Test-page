import React from "react";
import './sliderForList.scss';
interface SliderForListProps {
    list: string[];
    }

const SliderForList:React.FC<SliderForListProps>=({list})=>{
        return <div></div>
}
export default SliderForList;